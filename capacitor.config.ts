import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Trabajo tipo prueba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
